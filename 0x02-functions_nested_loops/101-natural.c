#include <stdio.h>

/**
 * main - prints the sum of all multiples of 3 or 5 to 1024
 *Return: Always (success)
 */
int main(void)
{
	int i, z = 0;

	while (i < 1024)
	{
	z += i;
	}
	i++
	}
	printf("%d\n", z);
	return (0);
}
