#include "main.h"

/**
 * mul - multiples two integer
 * @x: The first num to multiply
 * @y: The second num to multiply with first num
 * Return: The result of multiplication between x param and y param
 */
int mul(int x, int y)

{
	return (x * y);
}
